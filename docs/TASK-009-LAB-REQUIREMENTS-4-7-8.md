# TASK-009: закрыть требования 4, 7 и 8 ЛР1

## Цель

Довести минимально рабочую версию до требований лабораторной работы №1 по пунктам 4, 7 и 8.

## Объём

1. Пользовательский веб-интерфейс поверх существующего FastAPI.
2. Документация схемы данных и инструкции по реальному удалённому Git-репозиторию.
3. Воспроизводимый Pull Request / Merge Request-процесс с автоматическими проверками.

## Критерии готовности

- `GET /` отдаёт пользовательский интерфейс.
- Интерфейс использует HTTP API для справочников, рейсов, улова и отчёта.
- Ошибка бизнес-правила по грузоподъёмности отображается в интерфейсе.
- README содержит URL запуска UI, описание API и ссылку на схему данных.
- `docs/DATA_MODEL.md` содержит ER-схему и связи.
- `docs/REMOTE_REPOSITORY.md` содержит команды подключения GitHub/GitLab.
- `.github/workflows/ci.yml` запускает `make quality` и `make test` на push/PR.
- PR-шаблон требует проверки UI, API, секрета `.env` и результатов тестов.
